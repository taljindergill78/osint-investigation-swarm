[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app.app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
healthcheckPath = "/healthz"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
